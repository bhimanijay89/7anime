import { ChevronLeft, ChevronRight, Pause, Play } from 'lucide-react'
import { useEffect, useState } from 'react'
import { Button, IconButton } from '../ui/Button'
import './rail.css'
export interface Slide { title:string; description:string; image:string; eyebrow:string }
export function CinematicCarousel({slides}:{slides:Slide[]}){const [index,setIndex]=useState(0);const [paused,setPaused]=useState(false);useEffect(()=>{if(paused)return;const timer=window.setInterval(()=>setIndex(i=>(i+1)%slides.length),6500);return()=>window.clearInterval(timer)},[paused,slides.length]);const slide=slides[index];return <section className="cinematic" aria-roledescription="carousel" aria-label="Featured anime" style={{backgroundImage:`linear-gradient(90deg,#080a0f 5%,rgba(8,10,15,.5) 65%,#080a0f),url(${slide.image})`}}><div className="cinematic__body"><p>{slide.eyebrow}</p><h1>{slide.title}</h1><span>{slide.description}</span><Button>Explore title</Button></div><div className="cinematic__controls"><IconButton label="Previous slide" onClick={()=>setIndex((index-1+slides.length)%slides.length)}><ChevronLeft/></IconButton><IconButton label={paused?'Resume autoplay':'Pause autoplay'} onClick={()=>setPaused(!paused)}>{paused?<Play/>:<Pause/>}</IconButton><IconButton label="Next slide" onClick={()=>setIndex((index+1)%slides.length)}><ChevronRight/></IconButton></div><div className="cinematic__dots">{slides.map((s,i)=><button key={s.title} onClick={()=>setIndex(i)} aria-label={`Show ${s.title}`} aria-current={i===index}/>)}</div></section>}
